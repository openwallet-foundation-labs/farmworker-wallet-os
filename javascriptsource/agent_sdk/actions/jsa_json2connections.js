// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
import "mx-global";
import { Big } from "big.js";

// BEGIN EXTRA CODE
import "../shim.js";
import support from "../support/entidad";
// END EXTRA CODE

/**
 * @param {string} json
 * @returns {Promise.<MxObject[]>}
 */
export async function jsa_json2connections(json) {
	// BEGIN USER CODE
	try{
		let ret=[];
		let connections=JSON.parse(json);
		connections.forEach(async(connection)=>{
				let obj=await support.data.create({entity:"Agent_SDK.Connection"});
				ret.push(obj);
				obj.getAttributes().forEach((attr)=>{
					let _attr=attr;
					switch(_attr){
						case"id_":
							_attr="id";
							break;
						default:
							break;
					}
					let value=connection[_attr];
					if(typeof(value)=="undefined")return;
					if(obj.isReadonlyAttr(attr)){
						return;
					}
					else if(obj.isBoolean(attr)){
						obj.set(attr,value);
					}
					else if(obj.isDate(attr)){
						obj.set(attr,new Date(value));
					}
					else if(obj.isEnum(attr)){
						obj.set(attr,value);
					}
					else if(obj.isLocalizedDate(attr)){
						obj.set(attr,new Date(value));
					}
					else if(obj.isNumeric(attr)){
						obj.set(attr,value);
					}
					else if(obj.isPassword(attr)){
						obj.set(attr,value);
					}
					else{//string
						obj.set(attr,value);
					}
				});
				let _=obj;
				obj=await support.data.create({entity:"Agent_SDK._tags"});
				obj.set("Agent_SDK._tags_Connection",_)
				obj.getAttributes().forEach((attr)=>{
					let value=connection[attr];
					if(typeof(value)=="undefined")return;
					switch(attr){
						case"id":
							attr="id_";
							break;
						default:
							break;
					}
					if(obj.isReadonlyAttr(attr)){
						return;
					}
					else if(obj.isBoolean(attr)){
						obj.set(attr,value);
					}
					else if(obj.isDate(attr)){
						obj.set(attr,new Date(value));
					}
					else if(obj.isEnum(attr)){
						obj.set(attr,value);
					}
					else if(obj.isLocalizedDate(attr)){
						obj.set(attr,new Date(value));
					}
					else if(obj.isNumeric(attr)){
						obj.set(attr,value);
					}
					else if(obj.isPassword(attr)){
						obj.set(attr,value);
					}
					else{//string
						obj.set(attr,value);
					}
				});
				_=obj;
				connection.connectionTypes.forEach(async(connectionType)=>{
					obj=await support.data.create({entity:"Agent_SDK.connectionTypes"});
					obj.set("Agent_SDK.connectionTypes__tags",_)
					obj.set("value",connectionType);				
				})
			});
			return Promise.resolve(ret);
	}catch(e){
		return Promise.reject(e.toString());
	}
	// END USER CODE
}
